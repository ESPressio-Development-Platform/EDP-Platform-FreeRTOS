# Composition

Each concrete provider declares Platform-domain offers and properties required by the abstract contract. Bootstrap selects and owns provider objects; this repository does not introduce a FreeRTOS service registry.
