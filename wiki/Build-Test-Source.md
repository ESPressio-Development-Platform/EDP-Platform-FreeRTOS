# Build, Test and Source Map

C++20 is required along with a compatible FreeRTOS configuration. `docs/EXECUTION-CONTEXT.MD` and `docs/SYNCHRONIZATION.MD` are durable contracts. Tests and demos must be run against the same FreeRTOS configuration features that gate provider availability.
